enum ApiVersion {
  v1,
}

enum WidgetState {
  initial,
  loading,
  success,
  error,
}

enum Environment {
  dev,
  stage,
  production,
}

enum PaginatedWidgetState {
  initial,
  initialLoading,
  paginatedLoading,
  success,
  error,
  paginationError,
}
